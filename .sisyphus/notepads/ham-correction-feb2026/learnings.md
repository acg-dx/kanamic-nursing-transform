# Learnings — ham-correction-feb2026

## 2026-03-05 Session Start

### Key Architecture Facts
- `transcription.workflow.ts` is ~2000 lines. Key sections:
  - withRetry: lines 156-173
  - k2_3a flow: lines 413-420
  - updateTranscriptionStatus: ~630-640
  - selectQualificationCheckbox: 1808-1853
  - selectQualificationInFrame: 1859-1908 ← ROOT CAUSE BUG
  - deleteExistingSchedule: 1591-1670
  - record2flag check: 1631-1634
- `spreadsheet.service.ts`: column constants COL_A(0)..COL_Y(24) at lines 6-11
- `cjk-normalize.ts`: has normalizeCjkName(), needs extractPlainName() added
- `reconciliation.service.ts`: 超減算 filter at ~line 140

### Root Cause
selectQualificationInFrame() searches `name.includes('shikaku')` but HAM HTML uses `name="searchKbn"`.
Actual HTML: `<input type="radio" name="searchKbn" value="1" checked>看護師等`
Fix: select searchKbn radio → click `<input type="button" value="検索">` → wait for reload → selectServiceCode runs on filtered list.

### Business Rules
- 看護師 > 准看護師 priority (if both held, use 看護師)
- 看護医療 ONLY for qualification corrections (介護 not touched)
- 212 errors: 冨迫広美(123) + 永松アケミ(89)
- 有村愛・木場亜紗実: 0 errors (看護師, correct)

### Column Layout (BEFORE C1 insertion)
- S(18) = 転記フラグ
- T(19) = マスタ修正フラグ
- U(20) = エラー詳細
- V(21) = データ取得日時
- W(22) = サービス票チェック
- X(23) = 備考
- Y(24) = 実績ロック

### Column Layout (AFTER C1 insertion at index 18)
- S(18) = 加算対象の理由 ← NEW
- T(19) = 転記フラグ (was S)
- U(20) = マスタ修正フラグ (was T)
- V(21) = エラー詳細 (was U)
- W(22) = データ取得日時 (was V)
- X(23) = サービス票チェック (was W)
- Y(24) = 備考 (was X)
- Z(25) = 実績ロック (was Y)

### Sheet Info
- 姶良 sheetId: 12lzQUObLw0ymZdTRPpBWqECRkRimMXO7-m9maWPUt6M
- 転記フラグ pending values: '' | 'エラー：システム' | 'エラー：マスタ不備'(+T=TRUE) | '修正あり'
- 削除 tab status column: M(12), pending = not '削除済み' and not '削除不要'

## 2026-03-05 A4-A5 Implementation

### A4: 超減算/月超 Filter
- **Location**: reconciliation.service.ts, line ~151-152
- **Change**: Added filter to exclude HAM records where `serviceContent.includes('超減算') || serviceContent.includes('月超')`
- **Rationale**: These 15 records are auto-generated by HAM and are correct. They should NOT be flagged as "extra in HAM"
- **Expected Impact**: Reduces "extra in HAM" count from ~20 to ~5

### A5: SmartHR-Based Qualification Detection
- **Components Modified**:
  1. `reconciliation.service.ts`:
     - Added `staffQualifications: Map<string, string>` field
     - Added `setStaffQualifications(map)` method
     - Replaced CSV-based `junStaffSet` detection with SmartHR map lookup
     - Updated qualification mismatch logic: checks if actual qualification matches service content
  
  2. `run-reconciliation.ts`:
     - Added SmartHR service import
     - Added Step 2.5: Fetch all crews from SmartHR API
     - Build qualification map with 看護師 > 准看護師 priority rule
     - Pass map to reconciliation service before reconcile()
  
- **Priority Rule**: `const actual = hasKangoshi ? '看護師' : (hasJun ? '准看護師' : null)`
  - If staff has BOTH 看護師 and 准看護師, use 看護師
  - Matches SmartHR custom fields 資格1~資格8
  
- **Fallback**: If SMARTHR_ACCESS_TOKEN not set, logs warning and continues with CSV-based detection

### Verification
- TypeScript: ✓ PASSED (npx tsc --noEmit)
- Evidence: .sisyphus/evidence/A4-A5-tsc.txt
- No compilation errors or type issues

### Known Staff Qualifications (from context)
- 冨迫広美: 准看護師 only (123 mismatches expected to be fixed)
- 永松アケミ: 准看護師 only (89 mismatches expected to be fixed)
- 有村愛: 看護師 (0 errors, correct)
- 木場亜紗実: 看護師 (0 errors, correct)

## A2-A3 Fixes (2026-03-05)

### A2: deleteExistingSchedule() Error Handling
- **Change**: Line 1645-1646, changed `return false` to `throw new Error('record2flag=1: 記録書IIにより削除不可')`
- **Rationale**: When record2flag=1 (deletion blocked by 記録書II), the function should throw an error instead of silently returning false. This prevents the caller from thinking deletion succeeded when it actually failed.
- **Preserved**: The `return false` at line 1640 for "record not found" case remains unchanged.

### A3: Duplicate HAM Registration Prevention
- **Implementation**: Added `hamRegistrationState` Map to track which records have completed HAM registration
- **Flag Setting**: Set flag to true after Step 8 (act_do) at line 443
- **Early Exit**: Added check at line 339-345 to skip HAM registration steps if already complete
- **Logging**: Added warning log when skipping HAM registration on retry
- **Scope**: Flag is scoped to record.recordId, persisting across retry attempts within the same record

### TypeScript Verification
- `npx tsc --noEmit` passes with zero errors
- Evidence saved to `.sisyphus/evidence/A2-A3-tsc.txt`

